#include "main.h"
/**
 * print_alphabet - Make the alphabet
 * Return: Always 0 for (Success)
 */
void print_alphabet(void)
{

	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
	_putchar(a);
	}
	_putchar('\n');
	return (0);
}
